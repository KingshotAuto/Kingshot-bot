<UserControl x:Class="Bot.GUI.Views.ConfigView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">

    <UserControl.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="0,0,10,5"/>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Margin" Value="0,0,10,5"/>
            <Setter Property="MinWidth" Value="60"/>
        </Style>
        <Style TargetType="CheckBox">
            <Setter Property="Margin" Value="0,0,10,5"/>
        </Style>
    </UserControl.Resources>

    <GroupBox Header="Cycle Management" Margin="0,10" Padding="10">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Row 0 -->
            <TextBlock Text="Min Wait (min):" Grid.Row="0" Grid.Column="0"/>
            <TextBox Text="{Binding MinWaitTimeBetweenCyclesMinutes}"
                     ToolTip="Minimum 1 minute required to prevent memory leaks. Setting to 0 causes system instability."
                     Grid.Row="0" Grid.Column="1"/>

            <TextBlock Text="Max Wait (min):" Grid.Row="0" Grid.Column="2"/>
            <TextBox Text="{Binding MaxWaitTimeBetweenCyclesMinutes}"
                     ToolTip="Maximum wait time between cycles in minutes"
                     Grid.Row="0" Grid.Column="3"/>

            <!-- Row 1 -->
            <TextBlock Text="Shutdown After Cycle:" Grid.Row="1" Grid.Column="0"/>
            <CheckBox IsChecked="{Binding ShutdownEmulatorsAfterCycle}"
                      ToolTip="If checked, emulators will shut down after each cycle"
                      Grid.Row="1" Grid.Column="1" Grid.ColumnSpan="3"/>

            <!-- Row 2 -->
            <TextBlock Text="Max Cycles (0 = unlimited):" Grid.Row="2" Grid.Column="0"/>
            <TextBox Text="{Binding MaxCycles}"
                     ToolTip="Maximum number of cycles to perform. 0 means unlimited."
                     Grid.Row="2" Grid.Column="1" Grid.ColumnSpan="3"/>

            <!-- Row 3 -->
            <TextBlock Text="Max Troop Train Wait (minutes):" Grid.Row="3" Grid.Column="0"/>
            <TextBox Text="{Binding MaxTroopTrainWaitMinutes}"
                     ToolTip="Maximum time to wait for troop training before timing out"
                     Grid.Row="3" Grid.Column="1" Grid.ColumnSpan="3"/>
        </Grid>
    </GroupBox>
</UserControl>
